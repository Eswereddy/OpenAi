# Copy this file to .env and fill in your real key. .env is gitignored —
# it will never be committed, so it's safe to put a real secret in it.
#
# Only ONE of these three is needed. If more than one is set, priority is
# OpenAI > Anthropic > Groq (see ai-provider.js).
#
# Don't have a paid key handy? Groq is genuinely free, no card required:
#   1. Go to https://console.groq.com/keys
#   2. Sign in (Google/GitHub works) and click "Create API Key"
#   3. Paste it below as GROQ_API_KEY
# That's it — every AI feature in this app (summary, chatbot, action plan,
# document checklist) will start working immediately.

OPENAI_API_KEY=
OPENAI_SUMMARY_MODEL=gpt-5-mini

ANTHROPIC_API_KEY=
ANTHROPIC_SUMMARY_MODEL=claude-sonnet-5

GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
