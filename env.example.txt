# Copy this file to .env and fill in your real key(s). .env is gitignored —
# it will never be committed, so it's safe to put a real secret in it.
#
# Only ONE key is required to turn on every AI feature. Setting MORE than
# one gives you automatic failover: at runtime, Groq is tried FIRST, then
# OpenAI, then Anthropic — if the first one errors, times out, or hits a
# rate limit, the app automatically retries on the next one instead of
# falling back to the static template. See ai-provider.js for the exact
# retry/failover logic.
#
# Don't have a paid key handy? Groq is genuinely free, no card required —
# and it's the primary/fastest provider here, so it's the best one to start
# with:
#   1. Go to https://console.groq.com/keys
#   2. Sign in (Google/GitHub works) and click "Create API Key"
#   3. Paste it below as GROQ_API_KEY
# That's it — every AI feature in this app (summary, chatbot, action plan,
# document checklist) will start working immediately.

# 1st (primary) — free, fast, no card required. See console.groq.com/keys
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b

# 2nd (backup) — used automatically if Groq is unset or fails
OPENAI_API_KEY=
OPENAI_SUMMARY_MODEL=gpt-5-mini

# 3rd (backup) — used automatically if Groq and OpenAI are unset or fail
ANTHROPIC_API_KEY=
ANTHROPIC_SUMMARY_MODEL=claude-sonnet-5
